"""App configuration for the relocations app."""
from django.apps import AppConfig


class RelocationsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'relocations'
